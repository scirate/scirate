<div class="scites-count">
  <button class="btn count"><%= paper.scites_count %></button>
  <% if paper.scites_count > 0 %>
    <div class="sciters-dropdown">
      <% paper.sciters.each do |user| %>
        <%= link_to user.name, user %>
      <% end %>
    </div>
  <% end %>
</div>
