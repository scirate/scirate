<% provide :title, @paper.identifier %>
<table class="paper">
  <%= render 'full_paper' %>
  <tr>
    <td>
      <%= render( partial: 'scite_form', object: @paper, as: :paper ) if signed_in? %>
      <%= render 'sciters' %>
    </td>
  </tr>
</table>
