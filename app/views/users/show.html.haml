- provide :title, @user.name

.container.profile
  %table.table
    %tr
      %td
        %h1
          = gravatar_for @user
          = @user.name
  .row-fluid
    .span6
      %h3 Scites (#{@user.scites_count})
      %ul.nav.nav-tabs
        %li.active= link_to "Sort by Scite Date", url_for(params.merge(scite_order: :scited))
        %li= link_to "Sort by Publication Date", url_for(params.merge(scite_order: :published))
      = render @scited_papers
    .span6
      %h3 Comments (#{@user.comments_count})
      = render @comments
